  <h2>User info</h2>
  <%= attachment_image_tag user, :profile_image, :fill, 60, 60, fallback: "no_image.jpg" %>
  <table class="table">
    <tbody>
    <tr>
      <th>name</th>
      <th>
        <%= user.name %>
      </th>
    </tr>
    <tr>
      <th>introduction</th>
      <th>
        <%= user.introduction %>
      </th>
    </tr>
    <!--<tr>-->
    <!--    <th>フォロー中</th>-->
    <!--    <th>-->
    <!--      < %= link_to followings_user_path(user) do %>-->
    <!--        < %= user.followings.count %>人-->
    <!--      < % end %>-->
    <!--    </th>-->
    <!--  </tr>-->
    <!--  <tr>-->
    <!--    <th>フォロワー</th>-->
    <!--    <th>-->
    <!--      < %= link_to followers_user_path(user) do %>-->
    <!--         %= user.followers.count %>人-->
    <!--      < % end %>-->
    <!--    </th>-->
    <!--  </tr>-->
      <% unless current_user == user %>
        <tr>
          <th>
            <% if current_user.following?(user) %>
              <%= link_to "フォロー外す", user_relationships_path(user), method: :delete, class:"btn btn-outline-dark display-inline"%>
              <%= link_to "chatを始める", chat_rooms_path(user_id: user.id), method: :post, class:"btn btn-success display-inline mt-2" %>
            <% else %>
              <%= link_to "フォローする", user_relationships_path(user), method: :post, class:"btn btn-primary"%>
            <% end %>
          </th>
        </tr>
      <% end %>

    </tbody>
  </table>
  <div class="row">
  <%= link_to edit_user_path(user), method: :get do %>
    <i class="btn btn-outline-secondary btn-block fas fa-user-cog"></i>
  <% end %>
  </div>